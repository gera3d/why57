{
  "name": "why57-lead-intake-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types --config wrangler.lead-intake.jsonc --strict-vars false",
    "check": "wrangler deploy --config wrangler.lead-intake.jsonc --dry-run --env=\"\" --outdir .wrangler-dry-run --secrets-file .dev.vars.example && tsc --noEmit",
    "test": "vitest run && node --test test/google-apps-script.node.mjs",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.5",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
